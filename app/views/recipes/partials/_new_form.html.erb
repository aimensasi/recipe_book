
<div class="container">
  <div class="card">
    <!-- Info Card -->
    <div class="header">
      <h1>New Recipe</h1>
    </div>
    <div class="card-block info-card">

      <%= form_for @recipe, :method => "Post" do |f|%>

      <div class="form-group row">
      <br>
          <%= f.label :title, "Title", :class => "col-sm-1 col-form-label" %>
          <div class="col-sm-8">
            <%= f.text_field :title, :class => "form-control", :placeholder => "What is your recipe titile?" , :value => "#{@recipe.title}"%>
          </div>
      </div>

        <div class="form-group row">

          <%= f.submit "Create Recipe", :class => "btn btn-primary" %>
        </div>

      <% end %>
    </div>
    <!-- Info Card -->
  </div>
</div>
